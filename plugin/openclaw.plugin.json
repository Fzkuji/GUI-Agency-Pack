{
  "id": "gui-platform-detect",
  "name": "GUI Platform Detection",
  "description": "Auto-detects platform (macOS/Linux/Windows) and injects relevant GUI tool guidance into agent context. Part of GUI Agency Pack.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "verbose": {
        "type": "boolean",
        "description": "Include detailed platform guide (default: false, only summary)"
      }
    },
    "additionalProperties": false
  }
}
